# qgis backup
*.qgs~

# qgis temporary files - write ahead log and shared memory
*.gpkg-wal
*.gpkg-shm

# high-res DEM and its hillshade - too big for git, download from the GitHub release (see README)
data/rasters/32k/*.tif*

experiments
